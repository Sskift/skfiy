name: skfiy dogfood report
description: Report a real packaged-app skfiy dogfood run with reproducible evidence.
title: "[dogfood] "
labels:
  - skfiy
  - dogfood
body:
  - type: markdown
    attributes:
      value: |
        Use this for real packaged-app testing only. Run from `dist/skfiy.app` or an alpha zip generated by `npm run alpha:artifact`; do not report `npm start`, Vite, tmux, or direct helper-only runs as product evidence.

  - type: input
    id: alpha_manifest
    attributes:
      label: alpha manifest
      description: Path or attachment name for `.skfiy-alpha/skfiy-<version>-<commit>-macos-unsigned.json`.
      placeholder: .skfiy-alpha/skfiy-0.1.0-677d27d-macos-unsigned.json
    validations:
      required: true

  - type: input
    id: alpha_zip
    attributes:
      label: alpha zip
      description: Path or attachment name for the unsigned alpha zip.
      placeholder: .skfiy-alpha/skfiy-0.1.0-677d27d-macos-unsigned.zip
    validations:
      required: true

  - type: input
    id: commit_sha
    attributes:
      label: commit sha
      description: Commit SHA from the alpha manifest.
      placeholder: 677d27d1cced8e0fda09e3cd2786a901ddddde64
    validations:
      required: true

  - type: input
    id: smoke_artifact
    attributes:
      label: smoke artifact
      description: Path or attachment name for `npm run smoke:ghostty -- --output <path>` JSON evidence.
      placeholder: .skfiy-smoke/ghostty-matrix-9260.json
    validations:
      required: true

  - type: input
    id: voice_smoke_artifact
    attributes:
      label: voice smoke artifact
      description: Path or attachment name for `npm run smoke:voice -- --output <path>` JSON evidence.
      placeholder: .skfiy-smoke/voice-native.json
    validations:
      required: true

  - type: dropdown
    id: runner_has_tmux
    attributes:
      label: runnerHasTmux
      description: Copy this from the smoke artifact JSON.
      options:
        - "false"
        - "true"
    validations:
      required: true

  - type: dropdown
    id: screen_recording
    attributes:
      label: Screen Recording
      description: Permission state for the tested skfiy.app bundle.
      options:
        - granted
        - denied
        - not-determined
        - unknown
    validations:
      required: true

  - type: dropdown
    id: accessibility
    attributes:
      label: Accessibility
      description: Permission state for the tested skfiy.app bundle.
      options:
        - granted
        - denied
        - not-determined
        - unknown
    validations:
      required: true

  - type: dropdown
    id: microphone
    attributes:
      label: Microphone
      description: Permission state for the tested skfiy.app bundle.
      options:
        - granted
        - denied
        - not-determined
        - unknown
    validations:
      required: true

  - type: dropdown
    id: speech_recognition
    attributes:
      label: Speech Recognition
      description: Permission state when testing the native macOS ASR provider.
      options:
        - granted
        - denied
        - not-determined
        - unknown
        - not tested
    validations:
      required: true

  - type: dropdown
    id: asr_provider
    attributes:
      label: ASR provider
      description: Provider used for the reported voice turn.
      options:
        - doubao
        - browser
        - native-macos
        - not tested
    validations:
      required: true

  - type: dropdown
    id: computer_use_result
    attributes:
      label: Computer Use result
      description: Top-level `result` from the smoke artifact or product-path run.
      options:
        - passed
        - blocked
        - needs-user-confirmation
        - denied
        - failed
        - not tested
    validations:
      required: true

  - type: textarea
    id: screenshots
    attributes:
      label: before screenshot / after screenshot
      description: Paste before screenshot and after screenshot paths from replay evidence when Computer Use passes; write `not available` when the run is permission-blocked before screenshots.
      placeholder: |
        before screenshot: /tmp/skfiy/ghostty-before-...
        after screenshot: /tmp/skfiy/ghostty-after-...
    validations:
      required: true

  - type: textarea
    id: voice_and_stop
    attributes:
      label: Voice and panic stop notes
      description: Include start/stop behavior, provider failure messages, and whether panic stop / global stop hotkey was tested.
      placeholder: |
        ASR provider:
        transcript observed:
        panic stop:
    validations:
      required: true

  - type: textarea
    id: reproduction
    attributes:
      label: Reproduction steps
      description: Include the exact launch, smoke, and alpha artifact commands used.
      placeholder: |
        npm run build
        npm run smoke:ghostty -- --matrix --output .skfiy-smoke/ghostty-matrix.json
        npm run smoke:voice -- --output .skfiy-smoke/voice-native.json
        npm run alpha:artifact -- --smoke-artifact .skfiy-smoke/ghostty-matrix.json
    validations:
      required: true

  - type: textarea
    id: expected_actual
    attributes:
      label: Expected vs actual
      description: What should skfiy have done, and what happened instead?
    validations:
      required: true
