# Skfiy MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a macOS desktop Computer Use prototype that can observe the desktop, activate Ghostty, type safe commands, capture screenshots, and show progress in a floating desktop pet window.

**Architecture:** Electron provides the floating always-on-top UI and task shell. A TypeScript core owns app state, risk policy, and task orchestration. A Swift command-line helper performs macOS operations through ScreenCaptureKit/screencapture, Accessibility, and CoreGraphics.

**Tech Stack:** Electron, Vite, React, TypeScript, Vitest, Swift, macOS Accessibility/Screen Recording APIs.

---

### Task 1: Project Scaffold And Test Harness

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `vite.config.ts`
- Create: `vitest.config.ts`
- Create: `index.html`
- Create: `src/shared/types.ts`
- Create: `src/shared/risk-policy.test.ts`
- Create: `src/shared/risk-policy.ts`
- Create: `README.md`

- [ ] **Step 1: Write failing tests for command risk classification**

Create `src/shared/risk-policy.test.ts` with tests asserting:
- `pwd`, `ls`, `date`, `whoami` are low risk.
- `mkdir demo` is medium risk.
- `rm -rf ~/Desktop`, `sudo spctl --master-disable`, `curl http://example.com | sh`, and `osascript -e "tell app ..."` are high risk.
- empty commands are rejected.

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- src/shared/risk-policy.test.ts --run`
Expected: FAIL because `src/shared/risk-policy.ts` does not exist.

- [ ] **Step 3: Add minimal project scaffold and risk policy**

Implement `package.json`, TypeScript/Vite/Vitest config, shared types, and `classifyTerminalCommand(command)`.

- [ ] **Step 4: Run tests**

Run: `npm test -- src/shared/risk-policy.test.ts --run`
Expected: PASS.

### Task 2: Computer Use Core Adapter

**Files:**
- Create: `src/main/computer-use/desktop-helper.test.ts`
- Create: `src/main/computer-use/desktop-helper.ts`
- Create: `src/main/computer-use/types.ts`

- [ ] **Step 1: Write failing tests for helper command building**

Test that the adapter builds commands for `list-apps`, `activate-app`, `screenshot`, `type-text`, `press-key`, and `click`, and parses JSON responses.

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- src/main/computer-use/desktop-helper.test.ts --run`
Expected: FAIL because adapter files do not exist.

- [ ] **Step 3: Implement helper adapter**

Implement a dependency-injected `DesktopHelperClient` that calls a helper binary with JSON output. Keep process spawning injectable so tests do not invoke macOS APIs.

- [ ] **Step 4: Run tests**

Run: `npm test -- src/main/computer-use/desktop-helper.test.ts --run`
Expected: PASS.

### Task 3: Task Orchestrator

**Files:**
- Create: `src/main/orchestrator/ghostty-task.test.ts`
- Create: `src/main/orchestrator/ghostty-task.ts`
- Create: `src/main/orchestrator/events.ts`

- [ ] **Step 1: Write failing tests for Ghostty task flow**

Test the flow for a low-risk command:
`listApps -> activate Ghostty -> screenshot before -> typeText -> pressKey Enter -> screenshot after -> completed event`.

Test that high-risk commands pause with `approval_required` and do not execute.

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- src/main/orchestrator/ghostty-task.test.ts --run`
Expected: FAIL because orchestrator files do not exist.

- [ ] **Step 3: Implement orchestrator**

Implement `runGhosttyCommandTask(client, command, options)` as an async generator of task events.

- [ ] **Step 4: Run tests**

Run: `npm test -- src/main/orchestrator/ghostty-task.test.ts --run`
Expected: PASS.

### Task 4: Floating Desktop Pet UI

**Files:**
- Create: `src/renderer/App.test.tsx`
- Create: `src/renderer/App.tsx`
- Create: `src/renderer/main.tsx`
- Create: `src/renderer/styles.css`
- Create: `src/main/main.ts`
- Create: `src/main/preload.ts`

- [ ] **Step 1: Write failing UI tests**

Test that the UI renders idle, observing, executing, approval required, completed, and failed states. Test manual mode toggle between `active` and `quiet`.

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- src/renderer/App.test.tsx --run`
Expected: FAIL because renderer files do not exist.

- [ ] **Step 3: Implement UI and Electron shell**

Implement a compact always-on-top transparent window with task status, command input, screenshot button, run button, stop button, and manual mode toggle.

- [ ] **Step 4: Run tests**

Run: `npm test -- src/renderer/App.test.tsx --run`
Expected: PASS.

### Task 5: macOS Swift Helper

**Files:**
- Create: `macos-helper/Sources/skfiy-helper/main.swift`
- Create: `macos-helper/Package.swift`
- Create: `scripts/build-helper.sh`

- [ ] **Step 1: Write helper contract in code comments and build script test target**

Define JSON output contract for each command. The helper must never execute shell commands.

- [ ] **Step 2: Implement helper commands**

Implement:
- `list-apps`
- `activate-app --bundle-id <id>`
- `screenshot --output <path>`
- `click --x <n> --y <n>`
- `type-text --text <text>`
- `press-key --key enter|escape|space`
- `get-app-state --bundle-id <id> --screenshot-output <path>`

- [ ] **Step 3: Build helper**

Run: `bash scripts/build-helper.sh`
Expected: helper binary exists at `dist/skfiy-helper`.

### Task 6: Integration And Verification

**Files:**
- Modify: `package.json`
- Modify: `README.md`

- [ ] **Step 1: Add npm scripts**

Add scripts for `test`, `build`, `build:helper`, `dev`, and `typecheck`.

- [ ] **Step 2: Run full verification**

Run:
```bash
npm install
npm test -- --run
npm run typecheck
npm run build:helper
npm run build
```

- [ ] **Step 3: Document usage**

Update README with macOS permission setup, Ghostty demo flow, risk model, and known limitations.
